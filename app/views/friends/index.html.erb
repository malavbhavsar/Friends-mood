<table class="table table-hover">
      <tr>
        <th>Name</th>
        <th>Status</th>
        <th>Mood</th>
      </tr>
      <% @friends.each do |f| %>
      <tr>
        <td><%= f["name"] %></td>
        <td><%= f["status"]["text"] %></td>
        <td><%= f["status"]["sentiment"] %></td>
      </tr>
      <%end %>
</table>

